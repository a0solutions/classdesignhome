/* tslint:disable:no-unused-variable */

import { CategorySubstrPipe } from './categorySubstr.pipe';

describe('Pipe: CategorySubstre', () => {
  it('create an instance', () => {
    const pipe = new CategorySubstrPipe();
    expect(pipe).toBeTruthy();
  });
});
