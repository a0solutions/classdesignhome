/* tslint:disable:no-unused-variable */
import { SpacesPipe } from './spaces.pipe';

describe('Pipe: Spacese', () => {
  it('create an instance', () => {
    const pipe = new SpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
